import { makeStyles } from '../../utils';

export const styles = makeStyles({
  file: {
    fontSize: 12,
    color: 'text.primary',
  },

  fileName: {
    fontSize: 14,
    fontWeight: 'bold',
    color: 'text.primary',
  },
});
