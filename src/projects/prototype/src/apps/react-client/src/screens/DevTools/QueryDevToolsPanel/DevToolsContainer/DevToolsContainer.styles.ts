import { makeSx } from '../utils';

export const styles = makeSx({
  root: {
    position: 'fixed',
    bottom: 0,
    left: 0,
    backgroundColor: '#242424',
    color: 'white',
    width: '100%',
    paddingBottom: 0,
  },
});
