export enum LogLevel {
  debug = 'debug',
  info = 'info',
  success = 'success',
  warn = 'warn',
  error = 'error',
}

export enum LogLevelNumber {
  debug = 4,
  info = 3,
  success = 2,
  warn = 1,
  error = 0,
}
